{
  "name": "powerbi-client-react-monorepo",
  "private": true,
  "workspaces": [
    "packages/react-client",
    "packages/token-api"
  ],
  "scripts": {
    "start": "concurrently \"npm:start --workspace=token-api\" \"npm:start --workspace=react-client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
